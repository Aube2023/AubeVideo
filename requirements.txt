Flask>=3.0.0
psycopg2-binary>=2.9.9
python-pam>=2.0.2
Pillow>=10.2.0
gunicorn>=21.2.0
python-dotenv>=1.0.0
Werkzeug>=3.0.0
bcrypt>=4.0.0         # vérif des mots de passe AubeMail (SSO écosystème)

# Optionnels (features avancées)
redis>=5.0.0          # cache + rate-limit + file de transcodage
rq>=1.16.0            # file de transcodage (worker supervisé)
pywebpush>=2.0.0      # web push VAPID (silencieux si absent)
stripe>=7.0.0         # tip jar (désactivé si absent)
pytest>=8.0.0         # tests
pytest-flask>=1.3.0   # fixtures Flask
